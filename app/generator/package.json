{
  "name": "gisamdg-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "ui5 build --dest dist --clean-dest && cp xs-app.json dist/ && rm -rf out && mkdir out && cd dist && zip -qr ../out/generator.zip . && cd .."
  },
  "devDependencies": {
    "@ui5/cli": "^4"
  }
}
